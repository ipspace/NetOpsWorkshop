This directory is still using the **provider** parameter that is
deprecated in recent Ansible releases.

You'll find examples updated to work with Ansible 2.7 (and network CLI)
in adjacent directories.
